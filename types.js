"use strict"
// Шаблонные строки
// важность: 5
// Что выведет этот скрипт?

let name = "Ilya";

alert( `hello ${1}` ); // ?  овтет hello 1

alert( `hello ${"name"}` ); // ? ответ hello name

alert( `hello ${name}` ); // ? ответ hello Ilya